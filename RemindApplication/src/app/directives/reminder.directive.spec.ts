import { ReminderDirective } from './reminder.directive';

describe('ReminderDirective', () => {
  it('should create an instance', () => {
    //const directive = new ReminderDirective();
    //expect(directive).toBeTruthy();
  });
});
